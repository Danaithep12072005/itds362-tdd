# REFLECTION

1. มีเทสต์ทั้งหมดกี่ตัวและโค้ดจริงกี่บรรทัด อัตราส่วนเป็นเท่าใด?
- มีเทสต์ทั้งหมด 6 ตัว
- โค้ดจริงในไฟล์ kitchen.py มีประมาณ 27 บรรทัด
- อัตราส่วนระหว่างบรรทัดโค้ดของเทสต์กับโค้ดจริง ใกล้เคียงกันคือประมาณ 1:1

2. ช่วงที่แถบเป็นสีแดงนานที่สุดคือเมื่อใด อะไรทำให้มันนาน?
- (ตอบตามจริงได้เลยครับ เช่น) ช่วงที่ทำขั้นตอน A6 เพราะมีการสร้างคลาสใหม่ถึง 2 คลาส (Sum และ Converter) และต้องเชื่อมการทำงานเข้าด้วยกัน ทำให้ต้องใช้เวลาคิดโครงสร้างก่อนจะทำเทสต์ให้ผ่านได้

3. หาจุดที่ใช้ Fake It หนึ่งจุด ใช้ Triangulate หนึ่งจุด และใช้ Obvious Implementation หนึ่งจุด เมื่อมองย้อนกลับไปแต่ละครั้งเลือกได้เหมาะสมหรือไม่?
- **Fake It:** ใช้ในขั้นตอน A1 ตอนที่ฮาร์ดโค้ด `self.amount = 600` ถือว่าเหมาะสมเพราะช่วยให้ได้แถบสีเขียวเร็วและระบบเชื่อมต่อกันได้จริง
- **Triangulate:** ใช้ในขั้นตอน A2 ที่เพิ่มเทสต์การคูณด้วย 2 เหมาะสมมากเพราะเป็นตัวบังคับให้ต้องลบเลข 600 ที่ฮาร์ดโค้ดไว้ทิ้งไปแล้วแทนที่ด้วยตัวแปร
- **Obvious Implementation:** ใช้ตอนเพิ่มหน่วย (unit) ในขั้นตอน A5 เพราะการสร้าง `__init__` มารับตัวแปรเพิ่ม เป็นเรื่องตรงไปตรงมาและมั่นใจว่าจะไม่พัง

4. ยังมีรายการใดค้างอยู่ในรายการทดสอบ รายการใดที่จะลงมือทำจริง และรายการใดที่ควรลบทิ้งเพราะเป็นการคาดเดาล่วงหน้า?
- **รายการที่ค้างอยู่:** การใช้ Dictionary (ความต้องการนิยาม `__hash__`)
- **ควรทำจริง:** การเพิ่มอัตราแปลงหน่วย (เพราะจำเป็นต้องใช้แปลง oz เป็น g)
- **ควรลบทิ้ง:** การจัดการกับประเภทตัวเลขที่ซับซ้อนขึ้นถ้าโจทย์ไม่ได้เรียกร้อง

5. ดูเมท็อด Quantity.reduce ถ้าออกแบบระบบทั้งหมดบนกระดาษก่อน นักศึกษาจะเขียนเมท็อดนี้ออกมาหรือไม่?
- (ตอบตามความรู้สึกได้เลยครับ เช่น) อาจจะไม่ได้เขียนออกมาแบบนี้แต่แรก เพราะการออกแบบบนกระดาษมักจะรวมทุกอย่างไว้ในคลาสเดียว แต่ TDD บังคับให้คิดถึงหน้าที่รับผิดชอบที่แยกกัน ทำให้เกิดการสร้างคลาส Sum ขึ้นมาแยกต่างหาก